Adjust the IP addresses, name of the Docker image and any other 
parameters for starting docker in rundocker.sh. Ideally, this should be done via an environment variabble.

Install the script rundocker.sh on the server in folder /etc/jupyter_desktop.
